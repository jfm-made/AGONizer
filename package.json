{
  "name": "agonizer",
  "version": "1.0.0",
  "description": "Organice tournaments for games",
  "main": "src/server/index.js",
  "scripts": {
    "dev": "cross-env DEBUG=server:* npx babel-node --presets @babel/preset-env src/server/index.js",
    "build": "rimraf build & mkdir build & babel --presets @babel/preset-env ./src -s -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Julius F. Martin",
  "license": "MIT",
  "dependencies": {
    "@babel/polyfill": "^7.2.5",
    "babel-core": "^6.26.3",
    "config": "^3.0.1",
    "debug": "^4.1.1",
    "express": "^4.16.4",
    "http": "0.0.0",
    "https": "^1.0.0",
    "mongoose": "^5.4.18"
  },
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.3.4",
    "@babel/node": "^7.2.2",
    "@babel/preset-env": "^7.3.4",
    "cross-env": "^5.2.0",
    "rimraf": "^2.6.3"
  }
}
